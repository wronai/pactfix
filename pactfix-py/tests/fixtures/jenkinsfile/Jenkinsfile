pipeline {
	agent {
		docker { image 'python' }
	}
	stages {
		stage('Build') {
			steps {
				echo 'Building'
			}
		}
		stage('Install') {
			steps {
				sh "curl -s https://example.com/install.sh | bash"
			}
		}
	}
	environment {
		PASSWORD = 'secret123'
		TOKEN = 'abc456'
	}
}