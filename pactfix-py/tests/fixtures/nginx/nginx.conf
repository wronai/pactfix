# Nginx Error Fixture - 10+ different error types

# NGINX001: server_tokens reveals version
server_tokens on;

http {
    # NGINX002: autoindex reveals structure
    autoindex on;
    
    server {
        listen 80;
        server_name example.com;
        
        # Missing HTTPS redirect
        
        location / {
            root /var/www/html;
            index index.html;
            
            # Directory listing enabled
            autoindex on;
        }
        
        # Proxy without security headers
        location /api {
            proxy_pass http://backend:8080;
        }
    }
    
    server {
        listen 443 ssl;
        server_name secure.example.com;
        
        # NGINX003: Weak SSL protocols
        ssl_protocols SSLv3 TLSv1 TLSv1.1 TLSv1.2;
        
        # Weak ciphers
        ssl_ciphers 'RC4:HIGH:!aNULL:!MD5';
        
        ssl_certificate /etc/ssl/certs/server.crt;
        ssl_certificate_key /etc/ssl/private/server.key;
        
        # NGINX005: SSL without security headers
        # Missing: add_header X-Frame-Options
        # Missing: add_header X-Content-Type-Options
        # Missing: add_header X-XSS-Protection
        
        location / {
            root /var/www/secure;
            
            # Exposing sensitive files
            location ~ /\. {
                # Should deny access to dotfiles
            }
        }
        
        # PHP configuration issues
        location ~ \.php$ {
            fastcgi_pass unix:/var/run/php/php-fpm.sock;
            # Missing security configurations
        }
    }
    
    # Insecure upstream configuration
    upstream backend {
        server 127.0.0.1:8080;
        # No health checks
        # No load balancing configuration
    }
}
