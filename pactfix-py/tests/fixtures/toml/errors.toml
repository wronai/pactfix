# TOML Error Fixture

	name = "app"   
	version = "1.0.0" 

[server]
port = 8080
host = "localhost"   

[database]
user = "admin"
password = "secret123"   

# Suspicious line (space in key)
invalid key = "value"
