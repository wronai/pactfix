# Kubernetes Deployment with security issues
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name: web
        image: nginx:1.25
        ports:
        - containerPort: 80
        # No resource limits
        # No probes defined
        env:
        - name: DATABASE_PASSWORD
        resources:
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
        resources:
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 250m
            memory: 256Mi
          initialDelaySeconds: 30
securityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 2000
securityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 2000
          periodSeconds: 10
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 250m
            memory: 256Mi
          value: "supersecret123"
        - name: API_KEY
          value: "sk-abcdef123456"
      - name: sidecar
        image: redis
        # Privileged container - very dangerous
        securityContext:
          privileged: true
          runAsUser: 0
        volumeMounts:
        - name: host-logs
          mountPath: /var/log/host
      volumes:
      - name: host-logs
        hostPath:
          path: /var/log
          type: Directory
---
# Pod with missing security context
apiVersion: v1
kind: Pod
metadata:
  name: worker-pod
spec:
  containers:
  - name: worker
    image: python:latest
    command: ["python", "app.py"]
    # Missing resource limits
    # Missing probes
    # Missing security context